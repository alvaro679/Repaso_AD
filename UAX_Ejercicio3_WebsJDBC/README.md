# UAX - Ejercicio 3 - Webs JDBC

Proyecto independiente para el ejercicio JDBC de busqueda en la tabla `webs` usando `LIKE ?`.

Para abrirlo en IntelliJ: `File -> Open` y selecciona esta carpeta. Ejecuta `src/Main.java`.

La conexion esta en `src/util/DBConnection.java`.

